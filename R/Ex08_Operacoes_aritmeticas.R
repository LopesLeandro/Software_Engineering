# ^ ou **   #Potenciação
#/          #Divisão
#*          #Multiplicação
#+          #Adição
#-          #Subtração

#Somar 2 números
1 + 1

#Subtrair 2 números
2 - 1

#Multiplicar 2 números
5 * 3

#Dividir 2 números
10 / 3

#Calculos complexos
(5 + 4) * 10
sin(5)


#Isolando partes de uma expressão
(4 + 2) * 3
4 + 2 * 3


#Algumas outras operações com R
abs(x)                      #Valor absoluto de x
log(x,b)                    #Logaritmo de x na base b
log(x)                      #Logaritmo natural de x
exp(x)                      #Exponencial elevado de x
sin(x)                      #Seno de x
cos(x)                      #Cosseno de x
tan(x)                      #Tangente de x
asin(x)                     #Arcoseno de x
round(x, digits = n)        #Arredonda x para n casas decimais
ceiling(x)                  #Arredonda x para cima
floor(x)                    #Arredonda x para baixo
trunc(x)                    #Trunca x
length(x)                   #Comprimento de x
sum(x)                      #Soma de x
prod(x)                     #Produto dos elementos do vetor x
max(x)                      #Seleciona maior valor do vetor x
min(x)                      #Seleciona menor valor do vetor x
range(x)                    #Retorna o menor e maior elemento do vetor x

#Exemplo de uso de funções
round(x, 3)
signif(x, 3)
